[project]
name = "almanac-api"
version = "0.1.0"
description = "Almanac Intelligence Platform API"
requires-python = ">=3.11"

[project.dependencies]
fastapi = ">=0.109.0"
uvicorn = { extras = ["standard"], version = ">=0.27.0" }
